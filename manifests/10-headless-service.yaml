apiVersion: v1
kind: Service
metadata:
  name: mongo-h
  namespace: ${NAMESPACE}
  labels:
    app: mongo
spec:
  clusterIP: None
  publishNotReadyAddresses: true
  selector:
    app: mongo
  ports:
    - port: 27017
      name: mongo
