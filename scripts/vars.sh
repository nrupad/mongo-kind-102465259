#!/usr/bin/env bash
# My student ID, set once here. Everything else (namespace, seed data)
# reads it from this file instead of me typing it in every script.
STUDENT_ID="102465259"
NAMESPACE="mongo-${STUDENT_ID}"
